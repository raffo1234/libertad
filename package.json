{
  "name": "galvez1519",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,ts,jsx,tsx,astro}": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.0.0"
  }
}
